package model

/**
  * POJO with the basic User Role information (Used by the User Role DAO, Service and Controller)
  */
case class UserRole(
                     id: Int,
                     role: String
                   )
